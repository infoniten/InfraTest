# Schema Registry - объяснение

## 🤔 Что такое Schema Registry?

**Schema Registry** - это сервис от Confluent для централизованного управления схемами данных в Apache Kafka.

## 🎯 Зачем нужен Schema Registry?

### 1. **Управление схемами**
- Централизованное хранение схем Avro, JSON Schema, Protobuf
- Версионирование схем при изменениях
- Валидация данных на уровне producer/consumer

### 2. **Совместимость данных**
- Проверка обратной/прямой совместимости при эволюции схем
- Предотвращение "breaking changes"
- Безопасная миграция структуры данных

### 3. **Оптимизация производительности**
- Компактная сериализация (особенно с Avro)
- Передача только ID схемы вместо полной схемы
- Уменьшение размера сообщений

## 📊 В нашем проекте

### Текущее состояние:
- **Формат данных**: JSON (простой, читаемый)
- **Schema Registry**: Не используется
- **Валидация**: На уровне приложения

### Когда стоит включить Schema Registry:

#### ✅ Включить, если:
- Планируете часто менять структуру сделок
- Нужна строгая валидация данных
- Важна оптимизация размера сообщений
- Множество producer/consumer приложений
- Требуется строгое управление API контрактами

#### ❌ Не нужен, если:
- Структура данных стабильна
- Простое тестирование/прототипирование
- Один источник данных (наш случай)
- JSON достаточно для ваших целей

## 🚀 Варианты запуска

### Минимальный (без Schema Registry)
```bash
make up-minimal
```
**Включает**: PostgreSQL, Redis, Kafka, Java Consumers

### Стандартный (с мониторингом)
```bash
make up
```
**Включает**: Все выше + Kafka UI, Prometheus, Grafana

### Полный (со Schema Registry)
```bash
make up-full
```
**Включает**: Все выше + Schema Registry

## 🔧 Если Schema Registry не запускается

### Типичные проблемы:

1. **Долгий старт** - нормально, может занимать 1-2 минуты
2. **Ошибка соединения с Kafka** - Kafka должен быть запущен первым
3. **Порт 8081 занят** - проверьте `lsof -i :8081`

### Решение:
```bash
# Запуск без Schema Registry (рекомендуется для тестирования)
make up-minimal

# Проверка логов Schema Registry
docker-compose logs schema-registry

# Полная остановка и перезапуск
make down
make up-full
```

## 📈 Пример использования Schema Registry

### 1. Регистрация схемы
```bash
curl -X POST -H "Content-Type: application/vnd.schemaregistry.v1+json" \
  http://localhost:8081/subjects/trades-value/versions \
  -d '{"schema": "{\"type\":\"record\",\"name\":\"Trade\",\"fields\":[{\"name\":\"tradeId\",\"type\":\"string\"},{\"name\":\"price\",\"type\":\"double\"}]}"}'
```

### 2. Java Producer с Avro
```java
@Configuration
public class KafkaConfig {
    @Bean
    public ProducerFactory<String, GenericRecord> producerFactory() {
        Map<String, Object> props = new HashMap<>();
        props.put(ProducerConfig.BOOTSTRAP_SERVERS_CONFIG, "localhost:9092");
        props.put(ProducerConfig.KEY_SERIALIZER_CLASS_CONFIG, StringSerializer.class);
        props.put(ProducerConfig.VALUE_SERIALIZER_CLASS_CONFIG, KafkaAvroSerializer.class);
        props.put("schema.registry.url", "http://localhost:8081");
        return new DefaultKafkaProducerFactory<>(props);
    }
}
```

## 🎯 Рекомендация для вашего проекта

**Для тестирования высокой нагрузки**: используйте `make up-minimal`
- Быстрый запуск
- Меньше ресурсов
- Фокус на производительности

**Для production-like тестирования**: используйте `make up-full`
- Полная экосистема
- Тестирование эволюции схем
- Мониторинг всех компонентов

---

## 🔗 Полезные ссылки

- [Schema Registry Documentation](http://docs.confluent.io/current/schema-registry/docs/index.html)
- [Avro Schema Evolution](https://docs.confluent.io/platform/current/schema-registry/avro.html)
- [Schema Registry REST API](https://docs.confluent.io/platform/current/schema-registry/develop/api.html)