# Изменения в архитектуре

## ✅ Schema Registry убран из основной конфигурации

### Что изменилось:
- **Schema Registry удален** из основного `docker-compose.yml`
- **Упрощена структура** - теперь один основной файл конфигурации
- **Ускорен запуск** - меньше сервисов, быстрее старт

### Новые команды:

```bash
# Основной запуск (рекомендуется)
make up              # PostgreSQL + Redis + Kafka + Consumers + Мониторинг

# С Schema Registry (если понадобится)
make up-with-schema  # Все выше + Schema Registry
```

### Что удалено:
- ❌ `make up-minimal`
- ❌ `make up-full`
- ❌ `make health-minimal`
- ❌ `docker-compose.minimal.yml`

### Что добавлено:
- ✅ `make up-with-schema` - запуск со Schema Registry
- ✅ `docker-compose.schema-registry.yml` - отдельный файл для Schema Registry
- ✅ Упрощенная структура команд

### Файлы в проекте:

| Файл | Назначение |
|------|------------|
| `docker-compose.yml` | **Основная конфигурация** (все нужные сервисы) |
| `docker-compose.schema-registry.yml` | Дополнительно Schema Registry |
| `SCHEMA_REGISTRY.md` | Документация по Schema Registry |

### Быстрый старт:

```bash
make build    # Сборка Java приложений
make up       # Запуск всех основных сервисов
make health   # Проверка
make producer-test  # Тестирование
```

🎉 **Теперь архитектура проще и быстрее запускается!**